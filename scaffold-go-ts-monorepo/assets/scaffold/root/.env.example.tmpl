# Docker Compose settings. Copy this file to .env to override defaults.
API_PORT=8080
WEB_PORT=5173
PROD_WEB_PORT=80

# Optional PostgreSQL connection. Empty keeps persistence disabled.
DATABASE_DSN=
CORS_ALLOW_ORIGINS=*
